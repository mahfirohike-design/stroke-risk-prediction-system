# stroke-risk-prediction-system
Machine learning-based stroke risk prediction system developed using medical record data from RSUD dr. Soeselo Tegal. The project includes data preprocessing, feature-label separation, train-test split, Pipeline (MinMaxScaler + SMOTE + KNN), hyperparameter optimization with GridSearchCV, model evaluation, and deployment using Streamlit.
